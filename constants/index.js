const EMAIL_REGEX = require('./emailRegex');
const DRINKS_CATEGORY = require('./drinksCategory');
const GLASSES = require('./glasses');
const DATE_REGEX = require('./dateRegex');

module.exports = {
  EMAIL_REGEX,
  DATE_REGEX,
  DRINKS_CATEGORY,
  GLASSES,
};
